<% provide(:title, "All categories") %>
<% provide(:btn_title, "Create category") %>
<%= render "shared/heading" %>
<div class="page-content container-fluid">
    <div class="page-content-item">
        <div class="row">
            <div class="col-md-4">
              <%= render('categories/form') %>
            </div>
            <div class="col-md-8">
                <table class="table cat-table">
                    <thead class="thead-dark">
                    <tr>
                        <th scope="col" class="w-5">#</th>
                        <th scope="col" class="w-20">Name</th>
                        <th scope="col" class="w-30">Description</th>
                        <th scope="col" class="w-20">Action</th>
                        <th scope="col" class="w-20">Parents</th>
                        <th scope="col" class="w-5">Count</th>
                    </tr>
                    </thead>
                    <tbody>
                        <%= render :partial => 'category', :collection => @categories_sort %>
                    </tbody>
                </table>
                <%= will_paginate @categories %>
            </div>
        </div>
    </div>
</div>